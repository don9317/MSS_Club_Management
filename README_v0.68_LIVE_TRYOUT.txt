MSS Club Management Suite v0.68 — LIVE TRYOUT VERSION

IMPORTANT:
- This version intentionally starts with ZERO players on a clean browser/site state.
- It does NOT automatically generate the 150 demo players.
- It does NOT inherit browser data from v0.67 or earlier versions.
- Import your real registration CSV under Registrations to begin.
- "Generate 150 Demo Players" remains available only as an optional testing tool.
- The demo CSV remains included as a support/test file, but is never loaded automatically.

CAMERA:
- Keeps the v0.67 webcam reliability changes:
  * any available webcam first
  * no facingMode requirement
  * 5-second timeout
  * visible error messages
  * Switch Camera when multiple cameras are available
  * Upload Photo fallback

WHY NO MIGRATION:
This is deliberate for tomorrow's live tryout so old sample registrations, sample teams,
evaluations, and other test data cannot contaminate the real event.
